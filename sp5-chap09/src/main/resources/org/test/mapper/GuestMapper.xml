<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.test.mapper.GuestMapper">

<!-- 	<select id="read" resultType="org.test.domain.GuestVO">
		select
		*
		from
		guest
		where userName =
		#{userName}
	</select>
 -->
	<select id="list" resultType="org.test.domain.GuestVO">
		SELECT * FROM GUEST
	</select>

</mapper>
